# app.css is generated-then-owned CSS from the Tailwind/DaisyUI removal
# (framework-free migration). It does not follow hand-authored lint rules yet;
# re-enable once the base/component/utility layers are refactored by hand.
resources/css/app.css
